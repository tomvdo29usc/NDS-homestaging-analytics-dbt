name: dbt CI

on:
  push:
    branches: [ main ]

jobs:
  dbt-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.11"

      - name: Install dbt
        run: |
          pip install dbt-bigquery

      - name: Create dbt profile
        run: |
          mkdir -p ~/.dbt
          
          echo '${{ secrets.DBT_USER_CREDS }}' > ~/profiles.json
          
          cat <<EOF > ~/.dbt/profiles.yml
          homestaging_analytics:
            target: prod
            outputs:
              prod:
                dataset: ${{ vars.BQ_HOMESTAGINGANALYTICS_DATASET }}
                job_execution_timeout_seconds: 300
                job_retries: 1
                location: ${{ vars.BQ_HOMESTAGINGANALYTICS_LOCATION }}
                type: bigquery
                method: service-account
                project: ${{ vars.BQ_HOMESTAGINGANALYTICS_PROJECT }}
                threads: 1
                keyfile: /home/runner/profiles.json
          EOF

      - name: Run dbt
        run: |
          dbt run
          dbt test
